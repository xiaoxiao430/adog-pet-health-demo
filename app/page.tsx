/* eslint-disable @next/next/no-img-element */
"use client";

import { useMemo, useState } from "react";

type TabKey = "home" | "booking" | "pets" | "shop" | "mine" | "add-pet" | "edit-pet";
type PetKey = string;
type TimelineFilter = "all" | "health" | "care" | "shop";
type IconName =
  | "bell" | "calendar" | "chevron" | "clock" | "cross" | "heart" | "home"
  | "chat" | "mic" | "paw" | "pet" | "plus" | "search" | "shop" | "sparkle" | "tag" | "user" | "x";

type Pet = {
  key: PetKey; name: string; species: string; breed: string; age: string; birthday: string;
  color: string; bg: string; initials: string; avatar: string; status: string; nextTask: string; nextTaskDate: string;
  weight: string; vaccine: string; deworming: string; medication: string; allergies: string;
  weightStatus: "偏瘦" | "正常" | "偏重"; lastVisit: string; examScore: string; examScoreDate: string; examScoreBasis: string; microchip: string; sterilized: string;
};
type TimelineItem = {
  id: string; date: string; eyebrow: string; title: string; body: string;
  type: "health" | "care" | "shop"; icon: IconName; accent: string; tag?: string; detail?: string;
};

const pets: Record<PetKey, Pet> = {
  abu: { key: "abu", name: "阿布", species: "犬", breed: "比熊 · 公", age: "3岁2个月", birthday: "2023年7月18日", color: "#f5b26e", bg: "#fff0df", initials: "阿", avatar: "/avatars/abu-avatar.png", status: "状态稳定", nextTask: "狂犬疫苗加强针", nextTaskDate: "9月28日到期", weight: "5.8 kg", weightStatus: "正常", vaccine: "9月28日", deworming: "9月03日", medication: "无长期用药", allergies: "未发现", lastVisit: "最近体检 · 8月29日", examScore: "92", examScoreDate: "2026年8月29日", examScoreBasis: "年度体检报告 · 体重 / 心肺 / 口腔 / 皮肤", microchip: "已登记 · ADG-ABU-023", sterilized: "已完成" },
  tangyuan: { key: "tangyuan", name: "汤圆", species: "猫", breed: "英短 · 母", age: "1岁8个月", birthday: "2024年1月6日", color: "#b797d9", bg: "#f0e8fa", initials: "汤", avatar: "/avatars/tangyuan-avatar.png", status: "需要留意耳道", nextTask: "耳道复查", nextTaskDate: "本周六建议复诊", weight: "3.9 kg", weightStatus: "正常", vaccine: "已完成", deworming: "9月08日", medication: "耳道护理中", allergies: "鸡肉待观察", lastVisit: "洗护观察 · 9月14日", examScore: "78", examScoreDate: "2026年8月20日", examScoreBasis: "幼猫体检报告 · 体重 / 耳道 / 口腔 / 粪便", microchip: "已登记 · ADG-TY-041", sterilized: "已完成" },
};

const timeline: Record<PetKey, TimelineItem[]> = {
  abu: [
    { id: "abu-care", date: "2026年9月12日", eyebrow: "洗护记录 · 爱多格洗护", title: "秋日轻盈修剪", body: "完成洗澡、修剪和基础皮肤观察，毛发状态良好。", type: "care", icon: "heart", accent: "#f08293", tag: "已完成", detail: "美容师小葵记录：耳缘、趾间和腹部皮肤未见明显异常，建议 28–35 天后安排下一次洗护。" },
    { id: "abu-vaccine", date: "2026年8月29日", eyebrow: "健康记录 · 爱多格景观路店", title: "年度体检完成", body: "体重 5.8kg，心肺听诊未见异常；已生成下一次疫苗提醒。", type: "health", icon: "cross", accent: "#2f9fbc", tag: "报告已归档", detail: "主治医生：周医生。体检摘要、疫苗批次和下次复查建议已经归档到阿布的健康时间线。" },
    { id: "abu-food", date: "2026年8月16日", eyebrow: "消费记录 · 爱多格商城", title: "主粮到店自提", body: "比熊小型犬粮 3kg × 1，已计入阿布的消费时间线。", type: "shop", icon: "shop", accent: "#d89b4c", tag: "已取货", detail: "订单 ADG-20260816-018。预计 30 天后进入复购提醒，可在商城中修改配送或自提方式。" },
  ],
  tangyuan: [
    { id: "tangyuan-care", date: "2026年9月14日", eyebrow: "洗护记录 · 爱多格洗护", title: "洗护时发现耳道偏红", body: "已上传近照并标记给医生，建议本周内完成耳道复查。", type: "care", icon: "heart", accent: "#f08293", tag: "待医生查看", detail: "美容师上传了 2 张近照并备注：右耳轻微发红，汤圆当日没有明显抓挠。建议医生完成耳道镜检查。" },
    { id: "tangyuan-check", date: "2026年8月20日", eyebrow: "健康记录 · 爱多格景观路店", title: "幼猫体检与驱虫", body: "完成体内外驱虫，医生建议 30 天后复查耳道和体重。", type: "health", icon: "cross", accent: "#2f9fbc", tag: "已完成", detail: "体检记录包含体重、耳道、口腔和粪便观察结果；医生建议持续记录饮食和排便状态。" },
    { id: "tangyuan-food", date: "2026年7月30日", eyebrow: "消费记录 · 爱多格商城", title: "猫砂与营养补充", body: "低尘猫砂 2 袋、化毛营养膏 1 支，支持下次周期提醒。", type: "shop", icon: "shop", accent: "#d89b4c", tag: "已取货", detail: "订单 ADG-20260730-006。化毛膏预计还有 12 天用完，可开启复购提醒。" },
  ],
};

const services = [
  { name: "问诊 / 体检", category: "医疗", note: "医生评估 · 30分钟", duration: "30分钟", price: 99, memberPrice: 79, staff: "周医生", location: "爱多格景观路店", benefit: "暖心会员立减 ¥20", icon: "cross" as IconName, color: "blue" },
  { name: "疫苗 / 驱虫", category: "医疗", note: "预防护理 · 可选时间", duration: "20分钟", price: 128, memberPrice: 108, staff: "李医生", location: "爱多格景观路店", benefit: "含免疫记录与提醒", icon: "heart" as IconName, color: "rose" },
  { name: "洗澡 / 美容", category: "洗护", note: "洗护套餐 · 约2小时", duration: "120分钟", price: 199, memberPrice: 169, staff: "美容师小葵", location: "爱多格宠物生活馆", benefit: "洗护次卡可抵 1 次", icon: "sparkle" as IconName, color: "gold" },
  { name: "复查 / 随访", category: "医疗", note: "接上一次服务记录", duration: "30分钟", price: 69, memberPrice: 49, staff: "周医生", location: "爱多格景观路店", benefit: "关联上次病历与照片", icon: "calendar" as IconName, color: "lilac" },
];
const shopItems = [
  { name: "爱多格精选主粮", category: "主粮", note: "低敏配方 · 阿布复购款", price: "¥128", memberPrice: "¥118", kind: "粮" },
  { name: "耳道护理套装", category: "护理", note: "医生建议 · 温和清洁", price: "¥68", memberPrice: "¥62", kind: "护" },
  { name: "洗护焕新次卡", category: "会员卡", note: "4次护理 · 送1次基础修剪", price: "¥399", memberPrice: "¥369", kind: "卡" },
];

function Icon({ name, size = 20, stroke = 1.8 }: { name: IconName; size?: number; stroke?: number }) {
  const common = { width: size, height: size, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: stroke, strokeLinecap: "round" as const, strokeLinejoin: "round" as const, "aria-hidden": true };
  switch (name) {
    case "home": return <svg {...common}><path d="m3 10 9-7 9 7" /><path d="M5 9.5V21h14V9.5" /><path d="M9 21v-7h6v7" /></svg>;
    case "calendar": return <svg {...common}><rect x="3" y="4.5" width="18" height="17" rx="3" /><path d="M8 2.5v4M16 2.5v4M3 9.5h18" /><path d="M8 13h.01M12 13h.01M16 13h.01M8 17h.01M12 17h.01" /></svg>;
    case "pet": return <svg {...common}><path d="M12 19.2c-3.8 0-6.8-2.1-6.8-4.8 0-2.3 2-3.7 3.6-4.7C9.8 9 10.5 8 12 8s2.2 1 3.2 1.7c1.6 1 3.6 2.4 3.6 4.7 0 2.7-3 4.8-6.8 4.8Z" /><ellipse cx="7.3" cy="6.4" rx="2" ry="2.7" transform="rotate(-25 7.3 6.4)" /><ellipse cx="16.7" cy="6.4" rx="2" ry="2.7" transform="rotate(25 16.7 6.4)" /><circle cx="4.4" cy="10.7" r="1.8" /><circle cx="19.6" cy="10.7" r="1.8" /></svg>;
    case "shop": return <svg {...common}><path d="M4 9.5h16l-1 11H5l-1-11Z" /><path d="M6 9.5 7.5 4h9L18 9.5M9 13h6M9 16.5h6" /></svg>;
    case "user": return <svg {...common}><circle cx="12" cy="8" r="3.5" /><path d="M5 21c.7-4 3-6 7-6s6.3 2 7 6" /></svg>;
    case "bell": return <svg {...common}><path d="M18 9a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4" /></svg>;
    case "chat": return <svg {...common}><path d="M20 11.5a7.5 7.5 0 0 1-8 7.5 8.7 8.7 0 0 1-3.2-.6L4 20l1.6-3.2A7.2 7.2 0 0 1 4 11.5 7.5 7.5 0 0 1 12 4a7.5 7.5 0 0 1 8 7.5Z" /><path d="M8.5 11.5h.01M12 11.5h.01M15.5 11.5h.01" /></svg>;
    case "mic": return <svg {...common}><rect x="8" y="3" width="8" height="12" rx="4" /><path d="M5 11a7 7 0 0 0 14 0M12 18v3M9 21h6" /></svg>;
    case "chevron": return <svg {...common}><path d="m9 5 7 7-7 7" /></svg>;
    case "clock": return <svg {...common}><circle cx="12" cy="12" r="9" /><path d="M12 7v5l3.5 2" /></svg>;
    case "cross": return <svg {...common}><path d="M9 3h6v6h6v6h-6v6H9v-6H3V9h6V3Z" /></svg>;
    case "heart": return <svg {...common}><path d="M20.8 8.9c0 5.4-8.8 10.1-8.8 10.1S3.2 14.3 3.2 8.9A4.6 4.6 0 0 1 12 6.5a4.6 4.6 0 0 1 8.8 2.4Z" /></svg>;
    case "paw": return <Icon name="pet" size={size} stroke={stroke} />;
    case "plus": return <svg {...common}><path d="M12 5v14M5 12h14" /></svg>;
    case "search": return <svg {...common}><circle cx="10.8" cy="10.8" r="6.8" /><path d="m16 16 4.5 4.5" /></svg>;
    case "sparkle": return <svg {...common}><path d="m12 3 1.5 5.5L19 10l-5.5 1.5L12 17l-1.5-5.5L5 10l5.5-1.5L12 3ZM19 16l.7 2.3L22 19l-2.3.7L19 22l-.7-2.3L16 19l2.3-.7L19 16Z" /></svg>;
    case "tag": return <svg {...common}><path d="M20 13 13 20 4 11V4h7l9 9Z" /><path d="M8 8h.01" /></svg>;
    case "x": return <svg {...common}><path d="m6 6 12 12M18 6 6 18" /></svg>;
  }
}
function AdogLogo() {
  return <img className="adog-logo" src="/brand/adog-logo.png" alt="爱多格 Logo" />;
}

function DoctorAvatar() {
  return <svg className="doctor-avatar-svg" viewBox="0 0 80 80" role="img" aria-label="在线咨询医生头像">
    <path d="M9 41c0-18 13-32 31-32s31 14 31 32-13 31-31 31S9 59 9 41Z" fill="#e5f6f5" />
    <path d="M19 72c2-13 10-20 21-20s19 7 21 20" fill="#fff" />
    <path d="M27 31c0-9 5-14 13-14s13 5 13 14v7c0 9-5 15-13 15s-13-6-13-15Z" fill="#ffd8c2" />
    <path d="M28 31c1-8 5-14 12-14 7 0 11 5 12 13-4-1-7-4-9-8-4 5-9 8-15 9Z" fill="#315b6d" />
    <path d="M27 26c1-7 6-11 13-11s12 4 13 11c-8-2-17-2-26 0Z" fill="#fff" stroke="#c9e7e8" strokeWidth="1.2" />
    <path d="M27 26c8-2 18-2 26 0" fill="none" stroke="#57aeb9" strokeWidth="2" strokeLinecap="round" />
    <path d="M34 51v8M46 51v8M30 59h20" fill="none" stroke="#57aeb9" strokeWidth="2" strokeLinecap="round" />
    <path d="M40 59c-9 0-10 10-3 12 6 2 10-3 8-8" fill="none" stroke="#57aeb9" strokeWidth="2" strokeLinecap="round" />
    <path d="M40 60v10M31 58l-4 13M49 58l4 13" fill="none" stroke="#d7eeee" strokeWidth="3" strokeLinecap="round" />
    <circle cx="40" cy="59" r="3" fill="#57aeb9" />
  </svg>;
}

function PetAvatar({ pet, size = "medium" }: { pet: Pet; size?: "small" | "medium" | "large" }) {
  const emoji = pet.species === "猫" ? "🐱" : "🐶";
  return <div className={`pet-avatar pet-avatar-${size} ${pet.avatar ? "has-image" : ""}`} style={{ background: pet.bg, color: pet.color }} aria-label={`${pet.name}，${pet.species}头像`}>{pet.avatar ? <img className="pet-avatar-image" src={pet.avatar} alt={`${pet.name}的头像`} /> : <span className="pet-emoji">{emoji}</span>}<i><Icon name="paw" size={size === "large" ? 20 : 13} stroke={1.5} /></i></div>;
}
function StatusPill({ children, tone = "blue" }: { children: React.ReactNode; tone?: "blue" | "rose" | "gold" | "neutral" }) {
  return <span className={`status-pill status-${tone}`}>{children}</span>;
}
function SectionHeading({ eyebrow, title, action, onAction }: { eyebrow?: string; title: string; action?: string; onAction?: () => void }) {
  return <div className="section-heading"><div>{eyebrow && <p className="eyebrow">{eyebrow}</p>}<h2>{title}</h2></div>{action && <button className="text-button" onClick={onAction}>{action}<Icon name="chevron" size={15} /></button>}</div>;
}
function PetSwitcher({ activePet, petMap = pets, onSelect, onAddPet }: { activePet: PetKey; petMap?: Record<PetKey, Pet>; onSelect: (pet: PetKey) => void; onAddPet?: () => void }) {
  return <div className="pet-switcher" aria-label="选择宠物">{(Object.keys(petMap) as PetKey[]).map((key) => { const pet = petMap[key]; return <button key={key} className={activePet === key ? "active" : ""} aria-pressed={activePet === key} onClick={() => onSelect(key)}><PetAvatar pet={pet} size="small" /><span>{pet.name}</span></button>; })}<button className="add-pet" aria-label="添加宠物" onClick={onAddPet}><Icon name="plus" size={17} /></button></div>;
}

function HealthSummaryCard({ pet, onOpenPets }: { pet: Pet; onOpenPets: () => void }) {
  return <section className="health-summary-card" aria-label={`${pet.name}健康摘要`}>
    <div className="health-summary-head"><div><p className="eyebrow">当前宠物 · 健康摘要</p><strong>{pet.status}</strong><span>{pet.lastVisit}</span></div></div>
    <div className="health-summary-grid"><span className="weight-metric"><strong>{pet.weight}</strong><small>体重 <em className={`weight-badge weight-${pet.weightStatus === "正常" ? "normal" : pet.weightStatus === "偏瘦" ? "low" : "high"}`} aria-label={`体重状态：${pet.weightStatus}`}>{pet.weightStatus}</em></small></span><span><strong>{pet.vaccine}</strong><small>疫苗</small></span><span><strong>{pet.deworming}</strong><small>驱虫</small></span><span><strong>{pet.allergies}</strong><small>过敏</small></span></div>
    <div className="health-summary-note"><span><b>用药</b>{pet.medication}</span><span><b>异常记录</b>{pet.status === "状态稳定" ? "暂无待处理异常" : "已有记录，建议查看"}</span></div>
    <button className="archive-entry" onClick={onOpenPets} aria-label={`进入${pet.name}宠物档案`}><span className="archive-entry-icon"><Icon name="pet" size={19} /></span><span className="archive-entry-copy"><strong>进入{pet.name}宠物档案</strong><small>健康时间线 · 基础信息 · 家庭协作</small></span><span className="archive-entry-arrow"><Icon name="chevron" size={17} /></span></button>
  </section>;
}

function HomeView({ pet, petMap, onPetSelect, onAddPet, onOpenBooking, onOpenPets, onOpenChat, onOpenShop }: { pet: Pet; petMap?: Record<PetKey, Pet>; onPetSelect: (pet: PetKey) => void; onAddPet?: () => void; onOpenBooking: (service?: string) => void; onOpenPets: () => void; onOpenChat: () => void; onOpenShop: () => void }) {
  return <div className="view-stack home-view">
    <header className="topbar"><div className="brand-lockup"><div className="brand-mark"><AdogLogo /></div><div><strong>爱多格</strong><span><b>@adog</b> · 宠物健康管家</span></div></div><button className="icon-button notification-button" onClick={onOpenChat} aria-label="打开消息"><Icon name="bell" size={20} /><i /></button></header>
    <PetSwitcher activePet={pet.key} petMap={petMap} onSelect={onPetSelect} onAddPet={onAddPet} />
    <HealthSummaryCard pet={pet} onOpenPets={onOpenPets} />
    <section className="hero-card appointment-card"><div className="hero-sun" /><div className="hero-copy"><div className="appointment-row"><span className="appointment-pet">{pet.name}</span><span className="appointment-copy"><strong>{pet.nextTask}</strong><span>{pet.nextTaskDate} · 爱多格景观路店</span></span><button className="hero-action" onClick={() => onOpenBooking("复查 / 随访")}>预约服务 <Icon name="chevron" size={13} /></button></div></div></section>
    <section><SectionHeading eyebrow="照护入口" title="今天想为它做点什么？" /><div className="quick-actions"><button onClick={() => onOpenBooking("问诊 / 体检")}><span className="quick-icon quick-blue"><Icon name="cross" size={21} /></span><strong>预约医疗</strong><small>问诊 · 体检 · 复查</small></button><button onClick={() => onOpenBooking("洗澡 / 美容")}><span className="quick-icon quick-rose"><Icon name="sparkle" size={21} /></span><strong>预约洗护</strong><small>洗澡 · 美容 · SPA</small></button><button onClick={onOpenShop}><span className="quick-icon quick-gold"><Icon name="shop" size={21} /></span><strong>逛逛商城</strong><small>精选好物 · 到店自提</small></button></div></section>
    <section className="story-card" onClick={onOpenChat} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); onOpenChat(); } }} role="button" tabIndex={0} aria-label="打开在线咨询"><div className="story-copy"><StatusPill tone="rose">健康助手 · 可转人工</StatusPill><h3>洗护发现异常，<br /><em>先上传照片问问医生</em></h3><p>助手会带上{pet.name}的档案上下文，帮你整理下一步。</p><span className="story-link"><Icon name="chat" size={14} /> 上传照片并咨询 <Icon name="chevron" size={14} /></span></div><div className="story-assistant-visual"><span className="doctor-avatar"><DoctorAvatar /></span><small>在线咨询</small></div></section>
    <section className="mini-insight"><div className="insight-icon"><Icon name="heart" size={19} /></div><div><p className="eyebrow">爱多格观察</p><strong>记录得越完整，复诊时越省心</strong><span>你可以随时补充体重、饮食和日常状态。</span></div></section>
  </div>;
}

function BookingView({ pet, initialService, onBack, onBooked }: { pet: Pet; initialService?: string; onBack: () => void; onBooked: () => void }) {
  const firstService = services.some((service) => service.name === initialService) ? initialService : services[0].name;
  const [selectedService, setSelectedService] = useState(firstService); const [selectedDate, setSelectedDate] = useState("周六 09/28"); const [selectedTime, setSelectedTime] = useState("10:30"); const [selectedLocation, setSelectedLocation] = useState("爱多格景观路店"); const [confirmed, setConfirmed] = useState(false); const [cancelled, setCancelled] = useState(false);
  const dates = ["周六 09/28", "周日 09/29", "周一 09/30"]; const times = ["09:00", "09:30", "10:00", "10:30", "11:00", "11:30", "12:00", "12:30", "13:00", "13:30", "14:00", "14:30", "15:00", "15:30", "16:00", "16:30", "17:00", "17:30", "18:00", "18:30", "19:00", "19:30", "20:00"]; const service = services.find((item) => item.name === selectedService) ?? services[0];
  if (cancelled) return <div className="view-stack centered-state"><div className="success-burst success-burst-neutral"><Icon name="calendar" size={36} /></div><p className="eyebrow">预约已取消</p><h1>这次先不赶时间，<br /><em>需要时再回来</em></h1><p>示例预约已从照护日历移除。真实版本会同步释放门店和医生时段。</p><button className="primary-button" onClick={onBack}>回到首页</button></div>;
  if (confirmed) return <div className="view-stack centered-state booking-confirmed"><div className="success-burst"><Icon name="heart" size={36} /></div><p className="eyebrow">预约成功 · 已加入照护日历</p><h1>{pet.name}会在<br /><em>{selectedDate} {selectedTime}</em><br />被好好照顾</h1><div className="booking-receipt-meta"><StatusPill tone="blue">预约成功</StatusPill><span>预约回执 · ADOG-{selectedDate.replace(/\D/g, "")}-{selectedTime.replace(":", "")}</span></div><div className="booking-confirm-card"><div><span>服务</span><strong>{service.name}</strong></div><div><span>门店 · {service.category}</span><strong>{selectedLocation}</strong></div><div><span>医生 / 美容师 · 时长</span><strong>{service.staff} · {service.duration}</strong></div><div><span>会员权益 · 价格</span><strong>{service.benefit} · ¥{service.memberPrice}</strong></div></div><p>服务前会再次提醒你，也可以在「我的预约」中修改或取消。</p><div className="confirmation-actions"><button className="secondary-button" onClick={() => setConfirmed(false)}>修改预约</button><button className="text-button cancel-link" onClick={() => setCancelled(true)}>取消预约</button><button className="primary-button" onClick={onBooked}>回到首页</button></div></div>;
  return <div className="view-stack booking-view"><header className="page-header"><button className="back-button" onClick={onBack} aria-label="返回首页"><Icon name="chevron" size={18} /></button><div><p className="eyebrow">为 {pet.name} 安排</p><h1>预约服务</h1></div><span className="header-step">1 / 2</span></header><div className="booking-pet-card"><PetAvatar pet={pet} size="medium" /><div><strong>{pet.name}</strong><span>{pet.breed} · {pet.age}</span></div><StatusPill tone="blue">已建档</StatusPill></div><section><SectionHeading eyebrow="第一步 · 选择服务" title="这次想预约什么？" /><div className="service-list">{services.map((item) => <button key={item.name} className={selectedService === item.name ? "selected" : ""} onClick={() => { setSelectedService(item.name); setSelectedLocation(item.location); }}><span className={`service-icon service-${item.color}`}><Icon name={item.icon} size={21} /></span><span><strong>{item.name}</strong><small>{item.note} · ¥{item.memberPrice} 会员价</small></span><i className="radio-dot" /></button>)}</div></section><section><SectionHeading eyebrow="第二步 · 门店与人员" title="让熟悉的人接住它" /><div className="booking-detail-grid"><button className="booking-detail-card selected" onClick={() => setSelectedLocation(service.location)}><span className="detail-icon"><Icon name="shop" size={17} /></span><span><small>门店</small><strong>{selectedLocation}</strong><em>距你 3.2km · 可预约</em></span></button><div className="booking-detail-card"><span className="detail-icon detail-rose"><Icon name="user" size={17} /></span><span><small>{service.category === "洗护" ? "美容师" : "医生"}</small><strong>{service.staff}</strong><em>{service.duration} · 服务记录会归档</em></span></div></div></section><section><SectionHeading eyebrow="第三步 · 日期与时间" title="留一段从容的时间" /><div className="date-row">{dates.map((date, index) => <button key={date} className={selectedDate === date ? "selected" : ""} onClick={() => setSelectedDate(date)}><span>{date.split(" ")[0]}</span><strong>{date.split(" ")[1]}</strong>{index === 0 && <small>推荐</small>}</button>)}</div><div className="time-heading"><span>可预约时间</span><small>每 30 分钟一个时间点</small></div><div className="time-grid">{times.map((time) => <button key={time} className={selectedTime === time ? "selected" : ""} onClick={() => setSelectedTime(time)}>{time}</button>)}</div></section><div className="booking-benefit"><div><p className="eyebrow">会员权益</p><strong>{service.benefit}</strong><span>原价 ¥{service.price} · 本次应付 ¥{service.memberPrice}</span></div><StatusPill tone="rose">已为你匹配</StatusPill></div><div className="booking-note"><Icon name="clock" size={16} /><span>提交后可在「我的预约」中修改或取消，服务前会再次提醒。医疗服务涉及诊断和用药时，以医生判断为准。</span></div><button className="booking-contact-card" onClick={() => onToast("企业微信联系入口将在对接后开启")}><span className="contact-avatar">企</span><span className="contact-copy"><strong>需要改时间或咨询？</strong><small>联系爱多格服务顾问 · 企业微信</small></span><span className="contact-action"><Icon name="chat" size={16} /> 联系</span></button><button className="primary-button sticky-action" onClick={() => setConfirmed(true)}>确认预约 · ¥{service.memberPrice} <Icon name="chevron" size={16} /></button></div>;
}

function PetFormView({ pet, onBack, onSave }: { pet?: Pet; onBack: () => void; onSave: (next: Pet) => void }) {
  const [name, setName] = useState(pet?.name ?? "");
  const [species, setSpecies] = useState<"犬" | "猫">(pet?.species === "猫" ? "猫" : "犬");
  const [breed, setBreed] = useState(pet?.breed?.split(" · ")[0] ?? "");
  const [birthday, setBirthday] = useState(() => {
    const match = pet?.birthday.match(/(\d{4})年(\d{1,2})月(\d{1,2})日/);
    return match ? `${match[1]}-${match[2].padStart(2, "0")}-${match[3].padStart(2, "0")}` : "";
  });
  const [weight, setWeight] = useState(pet?.weight?.replace(/\s?kg/i, "") ?? "");
  const [sex, setSex] = useState(pet?.breed?.split(" · ")[1] ?? "");
  const [saving, setSaving] = useState(false);
  const dateLabel = birthday ? (() => { const [year, month, day] = birthday.split("-"); return `${year}年${Number(month)}月${Number(day)}日`; })() : "待补充";
  const submit = (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    if (!name.trim() || !breed.trim() || !birthday || !weight.trim()) return;
    setSaving(true);
    window.setTimeout(() => {
      const normalizedWeight = `${Number(weight).toFixed(1)} kg`;
      const next: Pet = {
        key: pet?.key ?? `pet-${Date.now()}`,
        name: name.trim(), species, breed: `${breed.trim()}${sex ? ` · ${sex}` : ""}`, age: "新建档案", birthday: dateLabel,
        color: species === "猫" ? "#a78bda" : "#e7a25b", bg: species === "猫" ? "#f1e9fb" : "#fff0df", initials: name.trim().slice(0, 1), avatar: pet?.avatar ?? "", status: "待完善体检", nextTask: "首次体检", nextTaskDate: "建议尽快安排", weight: normalizedWeight, weightStatus: "正常", vaccine: "待录入", deworming: "待录入", medication: "暂无记录", allergies: "待观察", lastVisit: "尚未体检", examScore: "—", examScoreDate: "待体检", examScoreBasis: "完成首次体检后生成", microchip: pet?.microchip ?? `待登记 · ADG-${Date.now().toString().slice(-5)}`, sterilized: "待录入",
      };
      onSave(next);
    }, 260);
  };
  return <div className="view-stack pet-form-view"><header className="page-header page-header-wide"><button className="back-button" onClick={onBack} aria-label="返回宠物档案"><Icon name="chevron" size={18} /></button><div><p className="eyebrow">一宠一档 · {pet ? "编辑资料" : "新建资料"}</p><h1>{pet ? "编辑宠物档案" : "添加宠物"}</h1></div><span className="form-step">{pet ? "已建档" : "1 / 1"}</span></header><section className="pet-form-intro"><div className={`form-avatar ${species === "猫" ? "cat" : "dog"}`}><span>{species === "猫" ? "🐱" : "🐶"}</span></div><div><h2>{pet ? `更新${pet.name}的信息` : "把它加入爱多格"}</h2><p>先记录基础信息，之后可以继续补充疫苗、驱虫和体检报告。</p></div></section><form className="pet-form" onSubmit={submit}><label><span>宠物名字</span><input required value={name} onChange={(event) => setName(event.target.value)} placeholder="例如：可乐" /></label><div className="form-field"><span>宠物类型</span><div className="species-picker">{(["犬", "猫"] as const).map((item) => <button type="button" key={item} className={species === item ? "active" : ""} onClick={() => setSpecies(item)}>{item === "犬" ? "🐶" : "🐱"} {item}</button>)}</div></div><label><span>品种</span><input required value={breed} onChange={(event) => setBreed(event.target.value)} placeholder={species === "犬" ? "例如：比熊" : "例如：英短"} /></label><div className="form-two-col"><label><span>性别（选填）</span><select value={sex} onChange={(event) => setSex(event.target.value)}><option value="">未填写</option><option value="公">公</option><option value="母">母</option></select></label><label><span>生日</span><input required type="date" value={birthday} onChange={(event) => setBirthday(event.target.value)} /></label></div><label><span>当前体重（kg）</span><input required type="number" min="0.1" step="0.1" value={weight} onChange={(event) => setWeight(event.target.value)} placeholder="例如：4.8" /></label><p className="form-note"><Icon name="heart" size={15} /> 信息会用于生成提醒和照护记录，可随时在档案中修改。</p><button className="primary-button form-submit" type="submit" disabled={saving}>{saving ? "正在保存…" : pet ? "保存修改" : "保存并创建档案"} <Icon name="chevron" size={16} /></button></form></div>;
}

function PetsView({ activePet, petMap = pets, onSelectPet, onBook, onOpenChat, onEditPet, onToast }: { activePet: PetKey; petMap?: Record<PetKey, Pet>; onSelectPet: (key: PetKey) => void; onBook: () => void; onOpenChat: () => void; onEditPet?: () => void; onToast: (message: string) => void }) {
  const pet = petMap[activePet] ?? pets.abu; const [filter, setFilter] = useState<TimelineFilter>("all"); const [view, setView] = useState<"timeline" | "basic" | "family">("timeline"); const [selectedEvent, setSelectedEvent] = useState<TimelineItem | null>(null); const [showReport, setShowReport] = useState(false); const petTimeline = timeline[activePet] ?? timeline.abu; const items = useMemo(() => filter === "all" ? petTimeline : petTimeline.filter((item) => item.type === filter), [filter, petTimeline]);
  const petId = pet.microchip || `ADOG-${pet.key.toUpperCase()}`;
  return <div className="view-stack pets-view"><header className="page-header page-header-wide"><div><p className="eyebrow">Pet ID · {petId}</p><h1>宠物档案</h1></div><button className="icon-button" onClick={() => onSelectPet(activePet === "abu" ? "tangyuan" : "abu")} aria-label="切换宠物"><Icon name="pet" size={20} /></button></header><div className="pet-profile-card"><div className="profile-top"><PetAvatar pet={pet} size="large" /><div><h2>{pet.name}</h2><p>{pet.species} · {pet.breed} · {pet.age}</p><StatusPill tone={activePet === "tangyuan" ? "rose" : "blue"}>{pet.status}</StatusPill></div><button className="more-button" onClick={onEditPet} aria-label="编辑宠物档案">编辑</button></div><div className="profile-stats"><span><strong>{pet.birthday}</strong><small>生日</small></span><span><strong>{pet.examScore} 分</strong><small>体检得分</small></span><span><strong>已记录 {petTimeline.length || 0} 项</strong><small>照护事件</small></span></div></div><div className="pet-health-summary"><div className="exam-score"><strong>{pet.examScore}</strong><small>体检得分</small></div><div><p className="eyebrow">当前健康摘要</p><strong>{pet.status}</strong><span>{pet.lastVisit} · 异常记录已同步</span></div><StatusPill tone={activePet === "tangyuan" ? "rose" : "blue"}>{activePet === "tangyuan" ? "需要复查" : "状态稳定"}</StatusPill></div><div className="exam-assessment-card"><div className="exam-assessment-icon"><Icon name="cross" size={16} /></div><div className="exam-assessment-copy"><p className="eyebrow">体检得分依据</p><strong>{pet.examScoreDate} · {pet.examScoreBasis.split(" · ")[0]}</strong><span>{pet.examScoreBasis.split(" · ")[1]}</span></div><button className="text-button" onClick={() => setShowReport((current) => !current)}>查看报告 <Icon name="chevron" size={14} /></button><p className="exam-score-disclaimer">体检得分仅反映本次已检查项目，不等同于宠物整体健康状况；未检查疾病不在本次评分范围内。</p></div>{showReport && <section className="exam-report-panel"><div className="exam-report-head"><div><p className="eyebrow">体检报告 · 示例</p><h2>{pet.name}的体检记录</h2><span>{pet.examScoreDate} · {pet.examScoreBasis.split(" · ")[0]}</span></div><StatusPill tone="blue">已归档</StatusPill></div><div className="exam-report-grid"><div><small>体重 / 体态</small><strong>{pet.weight} · {pet.weightStatus}</strong></div><div><small>检查项目</small><strong>{pet.examScoreBasis.split(" · ")[1]}</strong></div><div><small>医生摘要</small><strong>{activePet === "abu" ? "心肺听诊未见明显异常" : "耳道需持续观察，建议复查"}</strong></div><div><small>下一步建议</small><strong>{pet.nextTask} · {pet.nextTaskDate}</strong></div></div><p className="exam-report-note">这是用于演示的结构化报告示例；实际得分只基于当次已检查项目，不能替代完整诊断。</p></section>}<div className="pet-tabs"><button className={view === "timeline" ? "active" : ""} onClick={() => setView("timeline")}>健康时间线</button><button className={view === "basic" ? "active" : ""} onClick={() => setView("basic")}>基础信息</button><button className={view === "family" ? "active" : ""} onClick={() => setView("family")}>家庭协作</button></div>{view === "timeline" && <section className="timeline-section"><SectionHeading eyebrow="成长与照护" title="每一次认真，都有迹可循" action="导出摘要" onAction={() => onToast(`${pet.name}的健康摘要已生成示例文件`)} /><div className="filter-row">{[["all", "全部"], ["health", "医疗"], ["care", "洗护"], ["shop", "消费"]].map(([key, label]) => <button key={key} className={filter === key ? "active" : ""} onClick={() => setFilter(key as TimelineFilter)}>{label}</button>)}</div><div className="timeline-list">{items.map((item) => <article className="timeline-item" key={item.id}><div className="timeline-line"><span style={{ background: item.accent }}><Icon name={item.icon} size={16} /></span></div><div className="timeline-body"><time>{item.date}</time><button className="timeline-card timeline-card-button" onClick={() => setSelectedEvent(item)}><div><p>{item.eyebrow}</p><h3>{item.title}</h3><span>{item.body}</span>{item.tag && <StatusPill tone={item.type === "health" ? "blue" : item.type === "care" ? "rose" : "gold"}>{item.tag}</StatusPill>}</div><Icon name="chevron" size={16} /></button></div></article>)}</div></section>}{view === "basic" && <section className="record-section"><SectionHeading eyebrow="一宠一档" title="基础信息都被好好记住" /><div className="info-grid"><div><small>生日</small><strong>{pet.birthday}</strong></div><div><small>体重</small><strong>{pet.weight}</strong></div><div><small>芯片 / Pet ID</small><strong>{pet.microchip}</strong></div><div><small>绝育状态</small><strong>{pet.sterilized}</strong></div><div><small>过敏</small><strong>{pet.allergies}</strong></div><div><small>长期用药</small><strong>{pet.medication}</strong></div><div><small>疫苗</small><strong>{pet.vaccine}</strong></div><div><small>驱虫</small><strong>{pet.deworming}</strong></div></div><div className="record-reminder"><Icon name="bell" size={18} /><div><strong>健康提醒</strong><span>{pet.nextTask} · {pet.nextTaskDate}</span></div><button onClick={onBook}>去预约 <Icon name="chevron" size={14} /></button></div></section>}{view === "family" && <section className="record-section"><SectionHeading eyebrow="家庭协作" title="一起照顾，信息不丢失" /><div className="family-card"><div className="family-avatars"><PetAvatar pet={pet} size="medium" /><span>＋</span><div className="family-member-avatar">林</div></div><strong>{pet.name}的家人</strong><p>你是管理员，可以邀请家人查看健康时间线、预约和消费记录。</p><div className="family-permissions"><span>健康记录</span><span>预约提醒</span><span>消费记录</span></div><button className="primary-button" onClick={() => onToast("邀请链接已生成示例，可在微信中分享")}>邀请家人 <Icon name="chevron" size={14} /></button></div></section>}<div className="timeline-cta"><div><strong>想把今天的状态也记下来？</strong><span>从一张照片或一句话开始就好。</span></div><button onClick={onOpenChat}><Icon name="plus" size={17} /> 新增记录</button></div><button className="primary-button" onClick={onBook}>为 {pet.name} 预约下一次服务 <Icon name="chevron" size={16} /></button>{selectedEvent && <div className="inline-detail"><div><p className="eyebrow">{selectedEvent.date} · {selectedEvent.eyebrow}</p><h3>{selectedEvent.title}</h3><p>{selectedEvent.detail ?? selectedEvent.body}</p><StatusPill tone={selectedEvent.type === "health" ? "blue" : selectedEvent.type === "care" ? "rose" : "gold"}>{selectedEvent.tag ?? "已记录"}</StatusPill></div><button className="icon-button" onClick={() => setSelectedEvent(null)} aria-label="关闭记录详情"><Icon name="x" size={18} /></button></div>}</div>;
}

function ShopView({ onAdd, onToast }: { onAdd: (name: string) => void; onToast: (message: string) => void }) {
  const [category, setCategory] = useState("全部"); const [delivery, setDelivery] = useState<"到店自提" | "配送">("到店自提"); const [searchOpen, setSearchOpen] = useState(false); const [cartCount, setCartCount] = useState(0); const [orderCreated, setOrderCreated] = useState(false); const categories = ["全部", "主粮", "护理", "会员卡"]; const visibleItems = category === "全部" ? shopItems : shopItems.filter((item) => item.category === category);
  const addItem = (name: string) => { setCartCount((count) => count + 1); onAdd(name); };
  return <div className="view-stack shop-view"><header className="topbar"><div><p className="eyebrow">爱多格自营 · {delivery}</p><h1>精选商城</h1></div><div className="shop-header-actions"><button className="icon-button" onClick={() => setSearchOpen((open) => !open)} aria-label="搜索商品"><Icon name="search" size={20} /></button><button className="cart-button" onClick={() => setOrderCreated(false)} aria-label="打开购物袋"><Icon name="shop" size={18} /><span>{cartCount}</span></button></div></header>{searchOpen && <div className="shop-search"><Icon name="search" size={16} /><input aria-label="搜索商品" placeholder="搜索主粮、护理和会员卡" autoFocus /></div>}<div className="shop-hero"><div><StatusPill tone="gold">本周照护提案</StatusPill><h2>把日常照顾，<br /><em>变成轻松习惯</em></h2><p>医生建议的好物，和每一次服务记录放在一起。</p></div><div className="shop-bowl">♡</div></div><div className="shop-categories">{categories.map((item) => <button key={item} className={category === item ? "active" : ""} onClick={() => setCategory(item)}>{item}</button>)}</div><div className="delivery-switch" aria-label="选择履约方式"><span>怎么收到？</span><button className={delivery === "到店自提" ? "active" : ""} onClick={() => setDelivery("到店自提")}>到店自提</button><button className={delivery === "配送" ? "active" : ""} onClick={() => setDelivery("配送")}>配送</button></div><section><SectionHeading eyebrow="为阿布和汤圆挑选" title="今天的温柔补给" action="查看订单" onAction={() => onToast("订单中心将在 P1 接入真实履约状态")} /><div className="product-list">{visibleItems.map((item, index) => <article className="product-card" key={item.name}><div className={`product-visual product-${index % 3}`}><span>{item.kind}</span></div><div className="product-info"><div className="product-title-row"><h3>{item.name}</h3><StatusPill tone="rose">会员价</StatusPill></div><p>{item.note}</p><div className="product-price"><strong>{item.memberPrice}</strong><del>{item.price}</del></div><button onClick={() => addItem(item.name)}><Icon name="plus" size={15} /> 加入购物袋</button></div></article>)}</div></section><div className="pickup-card"><div className="pickup-icon"><Icon name="shop" size={20} /></div><div><strong>{delivery === "到店自提" ? "到店自提更省心" : "配送到家更方便"}</strong><span>{delivery === "到店自提" ? "下单后到爱多格前台报手机号即可取货" : "满 ¥99 西安市内配送，预计明日送达"}</span></div><Icon name="chevron" size={16} /></div>{cartCount > 0 && <div className="shop-cart-bar"><div><span>购物袋 · {cartCount} 件</span><strong>会员权益已自动匹配</strong></div><button className="primary-button" onClick={() => { setOrderCreated(true); onToast(`示例订单已创建 · ${delivery}`); }}>去结算 <Icon name="chevron" size={14} /></button></div>}{orderCreated && <div className="order-status-card"><StatusPill tone="blue">订单已生成</StatusPill><strong>ADG-202609-021 · {delivery}</strong><span>商品会同步到阿布的消费记录，并在 30 天后提醒复购。</span></div>}</div>;
}

function MineView({ onOpenBooking, onOpenChat, onToast }: { onOpenBooking: () => void; onOpenChat: () => void; onToast: (message: string) => void }) {
  const [panel, setPanel] = useState<"booking" | "wallet" | "spend" | "family" | null>(null);
  return <div className="view-stack mine-view"><header className="topbar"><div><p className="eyebrow">我的爱多格</p><h1>陪伴，从被记住开始</h1></div><button className="icon-button" onClick={() => onToast("暂无新的消息") } aria-label="查看通知"><Icon name="bell" size={20} /></button></header><section className="member-card"><div className="member-glow" /><div className="member-top"><div><p>ADOG CARE</p><h2>暖心会员</h2></div><span>Lv.02</span></div><div className="member-bottom"><div><small>可用积分</small><strong>1,280</strong></div><div><small>本月已陪伴</small><strong>3 次</strong></div><button onClick={() => setPanel("wallet")}>查看权益 <Icon name="chevron" size={14} /></button></div></section><div className="mine-grid"><button onClick={() => setPanel("booking")}><span className="mine-icon mine-blue"><Icon name="calendar" size={20} /></span><strong>我的预约</strong><small>下周六 10:30</small></button><button onClick={() => setPanel("wallet")}><span className="mine-icon mine-rose"><Icon name="heart" size={20} /></span><strong>我的卡包</strong><small>洗护次卡 3 次</small></button><button onClick={() => setPanel("spend")}><span className="mine-icon mine-gold"><Icon name="tag" size={20} /></span><strong>消费记录</strong><small>本月 2 笔</small></button><button onClick={() => setPanel("family")}><span className="mine-icon mine-lilac"><Icon name="user" size={20} /></span><strong>家庭成员</strong><small>可邀请 2 人</small></button></div>{panel && <div className="mine-panel"><div className="mine-panel-head"><div><p className="eyebrow">我的服务</p><h2>{panel === "booking" ? "我的预约" : panel === "wallet" ? "卡包与权益" : panel === "spend" ? "消费记录" : "家庭成员"}</h2></div><button className="icon-button" onClick={() => setPanel(null)} aria-label="关闭详情"><Icon name="x" size={18} /></button></div>{panel === "booking" && <div className="mine-panel-list"><div><StatusPill tone="blue">已预约</StatusPill><strong>疫苗 / 驱虫 · 阿布</strong><span>周六 09/28 · 10:30 · 爱多格景观路店</span></div><button className="secondary-button" onClick={onOpenBooking}>修改预约</button></div>}{panel === "wallet" && <div className="mine-panel-list"><div><StatusPill tone="rose">剩余 3 次</StatusPill><strong>洗护焕新次卡</strong><span>有效期至 2026年12月31日 · 可抵基础洗护</span></div><div><StatusPill tone="gold">1,280 分</StatusPill><strong>暖心会员积分</strong><span>可兑换护理用品和服务优惠券</span></div></div>}{panel === "spend" && <div className="mine-panel-list"><div><strong>¥128 · 爱多格精选主粮</strong><span>2026年8月16日 · 到店自提 · 已归档至阿布</span></div><div><strong>¥399 · 洗护焕新次卡</strong><span>2026年8月03日 · 已核销 1 / 4 次</span></div><button className="text-button" onClick={() => onToast("消费明细导出将在 P1 开放")}>导出消费明细 <Icon name="chevron" size={14} /></button></div>}{panel === "family" && <div className="mine-panel-list"><div className="family-row"><div className="family-member-avatar">林</div><span><strong>林晓晓 · 管理员</strong><small>可管理全部档案和权益</small></span><StatusPill tone="blue">本人</StatusPill></div><button className="secondary-button" onClick={() => onToast("邀请链接已生成示例")}>邀请家庭成员</button></div>}</div>}<section className="care-plan"><SectionHeading eyebrow="本月照护计划" title="慢慢来，一项一项完成" /><div className="care-progress"><div className="progress-ring"><strong>68</strong><small>%</small></div><div><strong>已完成 4 / 6 项</strong><span>下一项：阿布的疫苗加强针</span><button onClick={onOpenBooking}>现在安排 <Icon name="chevron" size={14} /></button></div></div></section><button className="support-card" onClick={onOpenChat} aria-label="打开爱多格健康助手"><span className="support-face"><Icon name="chat" size={20} /></span><span className="support-copy"><strong>打开健康助手</strong><small>提问、上传照片，必要时可转人工</small></span><span className="support-arrow"><Icon name="chevron" size={17} /></span></button></div>;
}

function ChatSheet({ pet, onClose, onOpenBooking, onToast }: { pet: Pet; onClose: () => void; onOpenBooking: (service?: string) => void; onToast: (message: string) => void }) {
  const [messages, setMessages] = useState([{ from: "bot", text: `你好，我是爱多格小助手。现在正在查看 ${pet.name} 的档案，可以先帮你整理日常问题，再决定是否需要联系医生。` }]); const [draft, setDraft] = useState(""); const [isListening, setIsListening] = useState(false);
  const quickQuestions = ["洗护发现耳朵红怎么办？", `${pet.name}什么时候该打疫苗？`, "我想预约复查", "宠物一直喘、站不稳怎么办？"];
  const answer = (question: string) => { const urgent = question.includes("喘") || question.includes("站不稳"); const reply = urgent ? "这属于需要尽快线下判断的急症信号。请先保持环境安静，立即联系爱多格景观路店或附近 24 小时急诊，不要等待机器人判断。" : question.includes("耳朵") ? "如果出现明显红肿、异味、频繁抓挠或甩头，建议先不要自行用药。可以把近照发给医生，并预约耳道检查。" : question.includes("疫苗") ? `${pet.name} 的免疫记录显示下一项提醒为「${pet.nextTask}」，${pet.nextTaskDate}。建议提前预约，现场会由医生结合免疫记录确认。` : "可以的，我先为你打开预约服务。涉及诊断和用药的问题，会转给爱多格医生确认。"; setMessages((current) => [...current, { from: "user", text: question }, { from: "bot", text: reply }]); };
  const sendDraft = () => { const text = draft.trim(); if (!text) return; answer(text); setDraft(""); };
  const handleUpload = (event: React.ChangeEvent<HTMLInputElement>) => { if (!event.target.files?.length) return; setMessages((current) => [...current, { from: "user", text: `已上传照片：${event.target.files?.[0]?.name ?? "宠物近照"}` }, { from: "bot", text: "照片已加入本次咨询。小助手不会独立诊断，建议由医生结合照片和现场检查判断。" }]); event.target.value = ""; };
  const transferToHuman = () => { setMessages((current) => [...current, { from: "bot", text: "已为你标记人工接管。爱多格客服会结合宠物档案和本次对话继续跟进。" }]); onToast("已进入人工客服队列"); };
  const stopListening = () => { if (!isListening) return; setIsListening(false); setDraft("请帮我看看最近的健康记录。"); onToast("语音已转成文字，可编辑后发送"); };
  return <div className="sheet-backdrop" onClick={onClose}><aside className="chat-sheet" onClick={(event) => event.stopPropagation()}><div className="sheet-handle" /><header><div className="chat-title"><span className="chat-title-icon"><Icon name="chat" size={17} /></span><div><p className="eyebrow">知识库演示 · {pet.name} 的档案上下文</p><h2>爱多格健康助手</h2></div></div><button className="icon-button" onClick={onClose} aria-label="关闭智能客服"><Icon name="x" size={20} /></button></header><div className="chat-context"><PetAvatar pet={pet} size="small" /><span>当前宠物：<strong>{pet.name}</strong> · {pet.status}</span></div><div className="chat-messages">{messages.map((message, index) => <div key={`${message.from}-${index}`} className={`chat-message ${message.from}`}>{message.from === "bot" && <span className="bot-avatar"><Icon name="chat" size={14} /></span>}<p>{message.text}</p></div>)}</div><div className="chat-disclaimer"><strong>急症提示</strong> 出现呼吸困难、持续呕吐、抽搐、意识异常或站立不稳，请立即联系医院。涉及诊断和用药，请以医生判断为准。</div><div className="assistant-actions"><label className="attach-button"><input type="file" accept="image/*" aria-label="上传宠物照片" onChange={handleUpload} /><Icon name="plus" size={15} /> 上传照片</label><button className="handoff-button" onClick={transferToHuman}><Icon name="user" size={15} /> 转人工</button><button className="book-from-chat" onClick={() => { onClose(); onOpenBooking("复查 / 随访"); }}>预约复查 <Icon name="chevron" size={14} /></button></div><div className="question-chips">{quickQuestions.map((question) => <button key={question} onClick={() => answer(question)}>{question}</button>)}</div><form className="chat-composer" onSubmit={(event) => { event.preventDefault(); sendDraft(); }}><input value={draft} onChange={(event) => setDraft(event.target.value)} aria-label="输入你的宠物问题" placeholder="输入关于宠物的问题…" /><button type="button" className={`voice-button ${isListening ? "listening" : ""}`} aria-label="按住说话" aria-pressed={isListening} onPointerDown={() => setIsListening(true)} onPointerUp={stopListening} onPointerCancel={stopListening} onPointerLeave={stopListening} onKeyDown={(event) => { if ((event.key === "Enter" || event.key === " ") && !isListening) setIsListening(true); }} onKeyUp={(event) => { if (event.key === "Enter" || event.key === " ") stopListening(); }}><Icon name="mic" size={16} /></button><button type="submit" aria-label="发送问题"><Icon name="chevron" size={17} /></button></form>{isListening && <div className="voice-overlay" role="status" aria-live="polite"><div className="voice-orb"><Icon name="mic" size={26} /></div><strong>正在聆听…</strong><span>松开结束，自动转成文字</span><div className="voice-wave" aria-hidden="true"><i /><i /><i /><i /><i /><i /><i /></div></div>}</aside></div>;
}

export default function Home() {
  const [activeTab, setActiveTab] = useState<TabKey>("home"); const [activePet, setActivePet] = useState<PetKey>("abu"); const [petMap, setPetMap] = useState<Record<PetKey, Pet>>(pets); const [bookingService, setBookingService] = useState<string | undefined>(); const [showChat, setShowChat] = useState(false); const [toast, setToast] = useState<string | null>(null); const [showIntro, setShowIntro] = useState(true); const [introLeaving, setIntroLeaving] = useState(false);
  const currentPet = petMap[activePet] ?? petMap.abu; const openBooking = (service?: string) => { setBookingService(service); setActiveTab("booking"); }; const showToast = (message: string) => { setToast(message); window.setTimeout(() => setToast(null), 2800); };
  const savePet = (next: Pet) => { setPetMap((current) => ({ ...current, [next.key]: next })); setActivePet(next.key); setActiveTab("pets"); showToast(`${next.name}的档案已${petMap[next.key] ? "更新" : "创建"}`); };
  const enterApp = () => { if (introLeaving) return; setIntroLeaving(true); window.setTimeout(() => setShowIntro(false), 520); };
  const content = activeTab === "home" ? <HomeView pet={currentPet} petMap={petMap} onPetSelect={setActivePet} onAddPet={() => setActiveTab("add-pet")} onOpenBooking={openBooking} onOpenPets={() => setActiveTab("pets")} onOpenChat={() => setShowChat(true)} onOpenShop={() => setActiveTab("shop")} /> : activeTab === "booking" ? <BookingView pet={currentPet} initialService={bookingService} onBack={() => setActiveTab("home")} onBooked={() => { setActiveTab("home"); showToast("预约已加入你的照护日历"); }} /> : activeTab === "pets" ? <PetsView activePet={activePet} petMap={petMap} onSelectPet={setActivePet} onBook={() => openBooking("复查 / 随访")} onOpenChat={() => setShowChat(true)} onEditPet={() => setActiveTab("edit-pet")} onToast={showToast} /> : activeTab === "add-pet" ? <PetFormView onBack={() => setActiveTab("home")} onSave={savePet} /> : activeTab === "edit-pet" ? <PetFormView pet={currentPet} onBack={() => setActiveTab("pets")} onSave={savePet} /> : activeTab === "shop" ? <ShopView onAdd={(name) => showToast(`${name}已加入购物袋`)} onToast={showToast} /> : <MineView onOpenBooking={() => openBooking()} onOpenChat={() => setShowChat(true)} onToast={showToast} />;
  return <main className="prototype-stage"><div className="stage-orb stage-orb-blue" /><div className="stage-orb stage-orb-rose" /><div className="app-frame"><div className="app-scroll">{content}</div>{!(["booking", "add-pet", "edit-pet"] as TabKey[]).includes(activeTab) && <nav className="bottom-nav" aria-label="主导航">{([["home", "首页", "home"], ["booking", "预约", "calendar"], ["pets", "宠物档案", "pet"], ["shop", "商城", "shop"], ["mine", "我的", "user"]] as [TabKey, string, IconName][]).map(([key, label, icon]) => <button key={key} className={activeTab === key ? "active" : ""} aria-current={activeTab === key ? "page" : undefined} aria-label={`前往${label}`} onClick={() => setActiveTab(key)}><Icon name={icon} size={20} /><span>{label}</span></button>)}</nav>}</div><div className="stage-caption"><span>ADOG · PET CARE EXPERIENCE</span><p>让每一只宠物的成长，都被温柔地记住。</p></div>{showChat && <ChatSheet pet={currentPet} onClose={() => setShowChat(false)} onOpenBooking={openBooking} onToast={showToast} />}{toast && <div className="toast"><span>✓</span>{toast}</div>} {showIntro && <section className={`intro-screen ${introLeaving ? "is-leaving" : ""}`} aria-label="爱多格宠物健康管家欢迎页"><div className="intro-orb intro-orb-blue" /><div className="intro-orb intro-orb-rose" /><div className="intro-spark intro-spark-one">✦</div><div className="intro-spark intro-spark-two">✧</div><div className="intro-card"><div className="intro-logo-wrap"><AdogLogo /></div><p className="intro-kicker">ADOG · PET CARE EXPERIENCE</p><h1>让每一只宠物更健康，<br /><em>让每一天陪伴更美好</em></h1><p className="intro-copy">从一宠一档开始，记录照护与成长。</p><button className="intro-enter" onClick={enterApp}>进入爱多格 <Icon name="chevron" size={16} /></button><span className="intro-footnote">爱多格宠物健康管家</span></div></section>}</main>;
}
